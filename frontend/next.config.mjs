/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Remove the manual env object - let Next.js handle environment variables automatically
};

export default nextConfig;
